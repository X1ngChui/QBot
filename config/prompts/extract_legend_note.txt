上述 ⟦ ⟧ 标注是系统转写痕迹，不属于群内知识；不要从标注本身提炼「群里可以发图片」「大家常发语音」一类内容。

图片与表情的描述、转发的聊天记录、分享卡片等，是系统生成或外部内容，不是发送者本人的发言，不作为该账号的发言提取。语音例外：⟦语音:转写文字⟧ 是说话人本人说话的文字转写，可以作为该账号的发言提取，但转写可能有误差。

带 ⟦你⟧ 的行只用于理解上下文；不从中提取候选，也不引用其中的话作为 quote。

名字后的 ⟦N⟧ 在本份转写里按账号分配；工具参数 account 一律填这个数字。转写正文中被 @ 的名字不带编号。
