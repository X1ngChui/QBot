# Pinned to the exact versions production runs (captured from the live container,
# 2026-09-04). A rebuild must not change behavior by itself: ranges let any
# cache-busting build resolve a fresh minor release between two deploys that
# "changed nothing". Bump these on purpose, one at a time, and redeploy.
nonebot2[fastapi]==2.5.0
nonebot-adapter-onebot==2.4.6
nonebot-plugin-apscheduler==0.5.0

openai==3.8.0
httpx==0.28.1
# In-process speech recognition (the "sherpa" ASR backend): ONNX runtime included,
# no system deps. The model file itself lives outside the image - see
# scripts/fetch_asr_model.sh and the models/ mount in docker-compose.yml.
sherpa-onnx==1.13.7
asyncpg==0.31.0
pydantic==2.13.5
PyYAML==6.0.3
jieba==0.42.1
